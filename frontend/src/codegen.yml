overwrite: true
schema: "http://buysell_graphql-engine_1/v1/graphql"
documents: "queries/**/*.ts"
generates:
  types/graphqlSchema.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
